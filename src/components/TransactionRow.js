import React from "react";


function TransactionRow(props) {
  return (<tr>
                    <th>{props.order}</th>
                    
                </tr>
                );
}

export default TransactionRow;